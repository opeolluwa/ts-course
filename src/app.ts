/**
 * starting a ts project
 * migrating js app to ts
 * Object oriented javascript
 */

function addTwoNumbers(num1: number, num2: number): number {
  const res = num1 + num2;
  return res;
}

let resTwo = addTwoNumbers(2, 5);
console.log(resTwo);
